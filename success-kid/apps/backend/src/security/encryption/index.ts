/**
 * Encryption Module
 * 
 * Provides data encryption and key management services
 */
import { encryptionService } from './service';
import { EncryptionOptions, EncryptedData } from './types';

// Export types and service
export { encryptionService };
export type { EncryptionOptions, EncryptedData };
