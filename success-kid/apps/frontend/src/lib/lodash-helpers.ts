// Import from full lodash package
import { isNil, isFunction, isNaN, max } from 'lodash';

// Re-export the functions
export { isNil, isFunction, isNaN, max };

// This file provides the correct lodash utility functions
// to avoid the issue with default exports from individual lodash packages
