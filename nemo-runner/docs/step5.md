**Current_Step_Being_Detailed:**

*   **Step_Number:** `5`
*   **Step_Title:** `Enhance Player Character Visuals to Pixar Style (ClownfishAsset)`
*   **Step_Description_And_Key_Activities:** `Significantly refactor ClownfishAsset.ts. Develop advanced procedural geometry for a stylized clownfish body, expressive fins, and tail. Create custom GLSL shaders for iconic stripes, soft material quality, and expressive eyes (with specular highlights). Implement subtle procedural animations (fin/tail swim motion).`
*   **Primary_Component_s_Involved:** `ClownfishAsset.ts` (or player asset if named differently), `ProceduralAssetFactory.ts`, `ShaderManager.ts`, `src/lib/game/shaders/character/clownfish.vert.ts`, `src/lib/game/shaders/character/clownfish.frag.ts`, `PlayerController.ts` (for animation triggers/parameters), `ConfigurationSystem.ts` (`gameConfig.ts`).
*   **Key_Focus_Areas:** `Procedural Generation (Advanced), Shader Programming (GLSL), Animation (Shader-based or Skeletal if simple), Art Direction`
*   **Critical_Dependencies_For_This_Step:** `Current PlayerController using the asset. ShaderManager ready for custom shaders (from Step 2). Reference images.`
*   **Anticipated_Key_Output_Of_This_Step:** `Player character visually aligns with the target Pixar aesthetic (body shape, colors, stripes, expressive eyes). Includes subtle swimming animations. Performs well.`
*   **Strategic_Notes_Proactive_Considerations_And_Key_NFRs_To_Emphasize_For_This_Step:** `This is CRITICAL for "outstanding visuals." Iterate heavily on shaders. Focus on expressive eyes and smooth, organic forms. Animations should feel fluid. Reference provided images closely. Profile performance. NFRs: High visual quality, iconic look, performant.`

Here is the exhaustive, production-ready guide for **Step 5**:

---

### 0. Step Implementation Plan Identifier & Strategic Context

*   **Implementing Step from Blueprint:** `5: Enhance Player Character Visuals to Pixar Style (ClownfishAsset)`
*   **Phase:** `Phase 1 (Completion): Playable Core Game Prototype (in Next.js)`
*   **Blueprint Version:** `v1.0` (Generated by AI based on user's progress assessment)
*   **Date Prepared:** May 12, 2025
*   **A. Primary Goal & Anticipated Deliverables of THIS STEP:**
    *   This step aims to create the definitive visual representation of the player character ("Bubbles") by developing advanced procedural geometry and custom GLSL shaders that emulate the target Pixar aesthetic. Deliverables include a fully procedural, stylized clownfish mesh with iconic stripes, expressive eyes, soft material appearance, and subtle, fluid procedural swimming animations, all while maintaining good performance.
*   **B. Key Focus Areas & NFRs for THIS STEP:**
    *   **Key Focus Areas:** Procedural Generation (Advanced), Shader Programming (GLSL), Animation (Shader-based), Art Direction.
    *   **Strategic Notes & NFRs:**
        *   **High Visual Quality & Iconic Look (NFR):** The character is central to the game's appeal. The procedural model and shaders must capture the charm, roundness, and material qualities seen in the reference images. Iteration will be key.
        *   **Expressive Eyes:** The eyes are crucial for personality. Shaders will incorporate techniques for specular highlights, perhaps a slight parallax effect for depth, and clear iris/pupil definition.
        *   **Smooth, Organic Forms:** Procedural geometry generation will focus on avoiding sharp edges and achieving soft, appealing curves. Techniques like Catmull-Clark subdivision (if feasible performance-wise for a base mesh before shader displacement) or careful vertex placement and normal calculation will be important.
        *   **Fluid Procedural Animation:** Animations for fins and tail will be achieved primarily through vertex displacement in the vertex shader, driven by time and configurable parameters, to maintain the "purely procedural" goal for Phase 1 assets.
        *   **Performance (NFR):** The character mesh must be optimized (target polycount to be determined, but keep it reasonable, e.g., 1000-2500 triangles for a hero character on mobile web). Shaders must be efficient.

### 1. Prerequisites, Environment Setup & Configuration for THIS STEP

*   **A. Verification of Critical Dependencies:**
    1.  **`PlayerController.ts` using a placeholder mesh:**
        *   **Verification:** Confirm `PlayerController.ts` uses `ProceduralAssetFactory.createPlayerMesh()` and that this currently returns a simple placeholder (e.g., a sphere). (Status: Verified from current codebase).
    2.  **`ShaderManager.ts` ready for custom GLSL (Step 2):**
        *   **Verification:** Ensure `ShaderManager.ts` can load/register custom GLSL shader strings, preprocess includes, manage uniforms, and create `THREE.ShaderMaterial` instances. (Status: Assumed complete from Step 2 plan).
    3.  **Reference Images (Clownfish):**
        *   **Verification:** Have the provided clownfish images open and accessible as the primary visual guide.
    4.  **`gameConfig.ts` Player Visual Parameters (from previous step):**
        *   **Verification:** Open `src/lib/game/config/gameConfig.ts`. The `player` section with `clownFishBaseColor`, `clownFishStripeColor`, `eyePupilColor`, animation frequencies, etc., should be present. (Status: Verified).
*   **B. Required Software, Libraries, Tools & Versions:**
    *   Core Stack: Next.js, React, Three.js, TypeScript, GLSL.
    *   No new external libraries needed.
*   **C. Environment Configuration (Local & Target):**
    *   No new environment variables.
*   **D. Project Structure & Version Control Setup for THIS STEP:**
    *   **Current Git Branch:** Ensure you are on `feature/phase1-completion`.
    *   Create a new task branch:
        ```bash
        git checkout -b task/P1C-step5-clownfish-visuals feature/phase1-completion
        ```
    *   **New/Modified Files:**
        *   `src/lib/game/assets/character/ClownfishAsset.ts` (New, or significant refactor)
        *   `src/lib/game/shaders/character/clownfish.vert.ts` (New)
        *   `src/lib/game/shaders/character/clownfish.frag.ts` (New)
        *   `src/lib/game/assets/ProceduralAssetFactory.ts` (Modified to instantiate `ClownfishAsset`)
        *   `src/lib/game/services/ShaderManager.ts` (Modified to register clownfish shaders)
        *   `src/lib/game/config/gameConfig.ts` (Verify/utilize player visual parameters)

### 2. Detailed Implementation Guide & Production-Ready Code

---

#### Sub-Task 2.1: Design & Implement Procedural Geometry for Clownfish Body & Fins

*   **A. Purpose & Rationale:**
    *   To create the 3D mesh for the clownfish (body, dorsal fin, pectoral fins, pelvic fins, anal fin, caudal/tail fin) using procedural geometry techniques in Three.js, aiming for the characteristic rounded, organic shapes seen in the reference images.
*   **B. Implementation Details & Precise Instructions:**
    1.  Create `ClownfishAsset.ts`.
    2.  **Body:** Start with a base shape like a `CapsuleGeometry` or a stretched `SphereGeometry`. Modify its vertices to achieve the tapered, slightly ovoid clownfish body shape. Consider using NURBS or `LatheGeometry` for smoother initial forms if comfortable, then potentially convert to `BufferGeometry`. For P1 procedural purity, direct `BufferGeometry` manipulation or combining simpler geometries is preferred.
    3.  **Fins:** Create separate `BufferGeometry` for each fin type.
        *   **Dorsal, Anal, Pelvic Fins:** Can be constructed from custom shapes (e.g., `ShapeGeometry` extruded or with custom vertex placement) or modified planes.
        *   **Pectoral Fins:** Rounded, fan-like shapes.
        *   **Caudal Fin (Tail):** Characteristic rounded or slightly emarginate shape.
    4.  Combine all geometries into a single `THREE.Group` or merge into a single `BufferGeometry` if materials are uniform (though we'll likely want different shader effects or UV sections for different parts). Using a `Group` is often easier for animating parts separately if needed later, or for applying different material properties via shader logic based on vertex groups/attributes. We'll aim for a single mesh with vertex groups/attributes if possible for shader simplicity.
    5.  Pay close attention to vertex normals for smooth shading. Use `geometry.computeVertexNormals()` after any manual vertex manipulation, or set them manually for specific effects.
    6.  Generate UV coordinates carefully if planning to use any texture patterns or procedural texturing within the shader (e.g., for stripes, scales). For stripes, we might rely on world position or object-space position calculations in the shader initially.
*   **C. File Creation / Modification:**

    *   **Full File Path (New File):** `src/lib/game/assets/character/ClownfishAsset.ts`
    *   **File Content (Production-Ready Code - Initial Structure & Body Example):**
        ```typescript
        // src/lib/game/assets/character/ClownfishAsset.ts
        import * as THREE from 'three';
        import { ShaderManager } from '../../services/ShaderManager';
        import { configSystem } from '../../core/ConfigurationSystem';
        import { PlayerSettings } from '../../config/gameConfig'; // For player visual settings

        export class ClownfishAsset {
          private shaderManager: ShaderManager;
          public mesh!: THREE.Group; // Main group for all parts
          private playerConfig: Readonly<PlayerSettings>;

          // Animation parameters (can be driven by uniforms)
          private animationTime: number = 0;

          constructor(shaderManager: ShaderManager) {
            this.shaderManager = shaderManager;
            this.playerConfig = configSystem.get('player');
            this.createMesh();
          }

          private createBodyGeometry(): THREE.BufferGeometry {
            // Start with a sphere and deform it.
            // Parameters for a more clownfish-like ovoid body
            const bodyRadius = 0.4; // Base radius
            const bodyLengthFactor = 1.8; // How elongated the body is
            const bodyHeightFactor = 1.1; // How tall vs wide
            const radialSegments = 24; // More segments for smoother curves
            const heightSegments = 16;

            const geometry = new THREE.SphereGeometry(bodyRadius, radialSegments, heightSegments);
            const positions = geometry.attributes.position.array as Float32Array;

            // Deform the sphere into an ovoid clownfish body shape
            // Taper towards the tail, slightly flatten sides, round the top/bottom
            for (let i = 0; i < positions.length; i += 3) {
              const x = positions[i];
              const y = positions[i + 1];
              const z = positions[i + 2];

              // Normalized position (from -1 to 1 in Z, assuming sphere radius 1 before scaling)
              const normalizedZ = z / bodyRadius; // z is along the length here

              // Tapering factor: stronger tapering at the tail (positive Z)
              let taper = 1.0 - Math.pow(Math.max(0, normalizedZ * 0.8 + 0.1), 2.5); // Stronger taper towards tail
              taper = Math.max(0.2, taper); // Prevent collapsing to a point

              // Bulge slightly in the middle
              const bulge = 1.0 + Math.sin(Math.PI * (1.0 - Math.abs(normalizedZ * 0.9))) * 0.15;
              
              positions[i] *= taper * bulge * 0.9; // Slightly slimmer X
              positions[i+1] *= taper * bulge * bodyHeightFactor; // Taller Y
              positions[i+2] *= bodyLengthFactor; // Elongate along Z
            }
            geometry.attributes.position.needsUpdate = true;
            geometry.computeVertexNormals(); // Crucial for smooth shading
            return geometry;
          }

          private createFinGeometry(
            width: number, 
            height: number, 
            segmentsW: number = 3, 
            segmentsH: number = 4,
            shape: 'pectoral' | 'dorsal' | 'caudal' | 'pelvic' | 'anal' = 'pectoral'
          ): THREE.BufferGeometry {
            const geometry = new THREE.PlaneGeometry(width, height, segmentsW, segmentsH);
            const positions = geometry.attributes.position.array as Float32Array;

            // Deform plane to a fin shape (example for a generic fin, refine per type)
            for (let i = 0; i < positions.length; i += 3) {
                // Y is 'height' of fin, X is 'width'
                const x = positions[i]; // -width/2 to width/2
                const y = positions[i+1]; // -height/2 to height/2

                // Normalized coordinates
                const nx = (x / width) + 0.5; // 0 to 1
                const ny = (y / height) + 0.5; // 0 to 1

                if (shape === 'pectoral' || shape === 'pelvic') {
                    // Rounded fan shape for pectoral/pelvic
                    let taper = Math.sin(ny * Math.PI); // Strongest taper at base/tip
                    taper *= (1.0 - Math.pow(nx * 0.8, 2)); // Taper towards side edges
                    positions[i] *= taper * Math.cos(ny * Math.PI * 0.1); // Slight curve
                    if (ny < 0.2) positions[i] *= ny / 0.2; // Pinch the base
                } else if (shape === 'dorsal' || shape === 'anal') {
                    // Taper height towards front and back, more towards front
                    let taper = Math.sin(nx * Math.PI);
                    if (nx < 0.3) taper *= (nx / 0.3); // Sharper front
                    positions[i+1] *= taper * (1.0 - Math.pow(ny - 0.3, 2) * 0.5); // Curve the top edge
                } else if (shape === 'caudal') { // Tail fin
                    let taperY = Math.sin(ny * Math.PI); // Taper top/bottom
                    let taperX = 1.0 - Math.pow(Math.abs(nx - 0.2), 2.0); // Wider at base, tapers towards tip
                    if (nx < 0.1) taperX = Math.max(0.3, nx / 0.1); // Ensure base is not zero
                    positions[i] *= taperX * 1.2; // Make tail wider
                    positions[i+1] *= taperY;
                }
            }
            geometry.attributes.position.needsUpdate = true;
            geometry.computeVertexNormals();
            return geometry;
          }


          private createMesh(): void {
            this.mesh = new THREE.Group();
            this.mesh.name = "ClownfishPlayer";

            const bodyGeom = this.createBodyGeometry();
            // Material will be custom shader, placeholder for now
            const bodyMaterial = this.shaderManager.createShaderMaterial('clownfishShader', {
                // Uniforms will be set based on config + shader defaults
                uBaseColor: { value: new THREE.Color(this.playerConfig.clownFishBaseColor) },
                uStripeColor: { value: new THREE.Color(this.playerConfig.clownFishStripeColor) },
                uStripeEdgeColor: { value: new THREE.Color(this.playerConfig.clownFishStripeEdgeColor) },
                uFinAccentColor: { value: new THREE.Color(this.playerConfig.clownFishFinAccentColor) },
                uEyePupilColor: { value: new THREE.Color(this.playerConfig.eyePupilColor) },
                uEyeIrisColor: { value: new THREE.Color(this.playerConfig.eyeIrisColor) },
                uEyeHighlightColor: { value: new THREE.Color(this.playerConfig.eyeHighlightColor) },
            });
            
            if (!bodyMaterial) {
                console.error("ClownfishAsset: Failed to create clownfishShader. Using fallback.");
                const fallbackMat = new THREE.MeshPhongMaterial({ color: this.playerConfig.clownFishBaseColor });
                const body = new THREE.Mesh(bodyGeom, fallbackMat);
                this.mesh.add(body);
                return; // Stop here if shader fails
            }
            
            bodyMaterial.side = THREE.DoubleSide; // Good for fins if they are planes

            const body = new THREE.Mesh(bodyGeom, bodyMaterial);
            body.name = "ClownfishBody";
            this.mesh.add(body);

            // Create and position fins (example for dorsal and pectoral)
            // Sizes are approximate relative to bodyRadius = 0.4
            const dorsalFinGeom = this.createFinGeometry(0.7, 0.35, 5, 3, 'dorsal');
            const dorsalFin = new THREE.Mesh(dorsalFinGeom, bodyMaterial);
            dorsalFin.name = "DorsalFin";
            dorsalFin.position.set(0, 0.3, -0.1); // On top, slightly back
            dorsalFin.rotation.x = Math.PI * 0.05; // Slight angle
            this.mesh.add(dorsalFin);

            const pectoralFinGeom = this.createFinGeometry(0.3, 0.25, 3, 3, 'pectoral');
            const leftPectoralFin = new THREE.Mesh(pectoralFinGeom, bodyMaterial);
            leftPectoralFin.name = "LeftPectoralFin";
            leftPectoralFin.position.set(-0.3, -0.05, -0.2); // Side, slightly forward and down
            leftPectoralFin.rotation.y = -Math.PI / 2; // Pointing outwards
            leftPectoralFin.rotation.z = -Math.PI / 6; // Angled slightly down
            this.mesh.add(leftPectoralFin);

            const rightPectoralFin = new THREE.Mesh(pectoralFinGeom, bodyMaterial);
            rightPectoralFin.name = "RightPectoralFin";
            rightPectoralFin.position.set(0.3, -0.05, -0.2);
            rightPectoralFin.rotation.y = Math.PI / 2;
            rightPectoralFin.rotation.z = -Math.PI / 6;
            this.mesh.add(rightPectoralFin);
            
            const tailFinGeom = this.createFinGeometry(0.3, 0.45, 4, 3, 'caudal');
            const tailFin = new THREE.Mesh(tailFinGeom, bodyMaterial);
            tailFin.name = "TailFin";
            tailFin.position.set(0, 0.05, 0.65); // At the back
            this.mesh.add(tailFin);

            // Add other fins (pelvic, anal) similarly, positioning them appropriately.
            // For pelvic fins (usually paired, below pectorals)
            const pelvicFinGeom = this.createFinGeometry(0.2, 0.15, 3, 2, 'pelvic');
            const leftPelvicFin = new THREE.Mesh(pelvicFinGeom, bodyMaterial);
            leftPelvicFin.name = "LeftPelvicFin";
            leftPelvicFin.position.set(-0.15, -0.3, -0.05);
            leftPelvicFin.rotation.y = -Math.PI / 2;
            leftPelvicFin.rotation.z = Math.PI / 8;
            this.mesh.add(leftPelvicFin);

            const rightPelvicFin = new THREE.Mesh(pelvicFinGeom, bodyMaterial);
            rightPelvicFin.name = "RightPelvicFin";
            rightPelvicFin.position.set(0.15, -0.3, -0.05);
            rightPelvicFin.rotation.y = Math.PI / 2;
            rightPelvicFin.rotation.z = Math.PI / 8;
            this.mesh.add(rightPelvicFin);

            // Anal fin (single fin on the bottom, towards the tail)
            const analFinGeom = this.createFinGeometry(0.4, 0.2, 4, 2, 'anal');
            const analFin = new THREE.Mesh(analFinGeom, bodyMaterial);
            analFin.name = "AnalFin";
            analFin.position.set(0, -0.3, 0.3);
            analFin.rotation.x = -Math.PI * 0.05;
            this.mesh.add(analFin);


            // Rotate the whole fish to swim along negative Z
            this.mesh.rotation.y = Math.PI;
          }

          public getMesh(): THREE.Group {
            return this.mesh;
          }
          
          // This method will be called by PlayerController or GameEngine
          public updateAnimation(deltaTime: number, playerSpeed: number, isTurning: boolean, turnDirection: number): void {
            this.animationTime += deltaTime;
            const config = this.playerConfig;

            // Update uniforms for shader-based animation
            const bodyMaterial = (this.mesh.getObjectByName("ClownfishBody") as THREE.Mesh)?.material as THREE.ShaderMaterial;
            if (bodyMaterial && bodyMaterial.uniforms) {
                if (bodyMaterial.uniforms.uTime) bodyMaterial.uniforms.uTime.value = this.animationTime;
                if (bodyMaterial.uniforms.uPlayerSpeed) bodyMaterial.uniforms.uPlayerSpeed.value = playerSpeed;
                if (bodyMaterial.uniforms.uIsTurning) bodyMaterial.uniforms.uIsTurning.value = isTurning ? 1.0 : 0.0;
                if (bodyMaterial.uniforms.uTurnDirection) bodyMaterial.uniforms.uTurnDirection.value = turnDirection;
            }

            // Example CPU-based fin animation if not done in shader:
            const tailFin = this.mesh.getObjectByName("TailFin") as THREE.Mesh;
            if (tailFin) {
                tailFin.rotation.y = Math.sin(this.animationTime * config.tailFinFrequency * (1 + playerSpeed * 0.1)) * config.tailFinAmplitude * (1 + playerSpeed * 0.05);
            }

            const leftPectoral = this.mesh.getObjectByName("LeftPectoralFin") as THREE.Mesh;
            if (leftPectoral) {
                leftPectoral.rotation.x = Math.sin(this.animationTime * config.pectoralFinFrequency  * (1 + playerSpeed * 0.1)) * config.pectoralFinAmplitude;
            }
            const rightPectoral = this.mesh.getObjectByName("RightPectoralFin") as THREE.Mesh;
            if (rightPectoral) {
                rightPectoral.rotation.x = Math.sin(this.animationTime * config.pectoralFinFrequency  * (1 + playerSpeed * 0.1) + Math.PI * 0.1) * config.pectoralFinAmplitude; // Slightly offset phase
            }
          }

          public dispose(): void {
            this.mesh.traverse(child => {
              if (child instanceof THREE.Mesh) {
                child.geometry.dispose();
                if (Array.isArray(child.material)) {
                  child.material.forEach(m => m.dispose());
                } else {
                  child.material.dispose();
                }
              }
            });
          }
        }
        ```    *   **Explanation:**
        *   `ClownfishAsset` will manage a `THREE.Group` containing the body and fin meshes.
        *   `createBodyGeometry`: Deforms a sphere to get the main body shape.
        *   `createFinGeometry`: A helper to create plane-based fins, deformed based on `shape` type. This is a starting point; fins will need more detail.
        *   The main material will be a custom `clownfishShader` (defined next).
        *   `updateAnimation`: Will update uniforms for shader-based animation or directly manipulate fin rotations for CPU-based animation. Player speed and turning state will influence animations.

---

Okay, we've laid out the `ClownfishAsset.ts` structure with procedural geometry for the body and fins. Now, let's create the custom GLSL shaders that will bring "Bubbles" to life with the Pixar aesthetic, focusing on material properties, iconic stripes, and expressive eyes.

---

#### Sub-Task 2.2: Develop Custom GLSL Shaders for Clownfish (`clownfish.vert.ts`, `clownfish.frag.ts`)

*   **A. Purpose & Rationale:**
    *   To create vertex and fragment shaders that define the appearance of the clownfish, including its base color, white stripes with dark edges, fin accents, eye details (iris, pupil, specular highlight), and simulate a soft, slightly subsurface-scattering-like material quality. Animations for fins and tail will also be handled in the vertex shader.
*   **B. Implementation Details & Precise Instructions:**
    1.  **Vertex Shader (`clownfish.vert.ts`):**
        *   Pass UVs, normals, and world position to the fragment shader.
        *   Implement procedural animation for fins and tail:
            *   Use `uTime` and configurable frequencies/amplitudes (from `PlayerSettings`).
            *   Identify vertices belonging to different fins (e.g., by adding a vertex attribute during geometry creation, or by checking `uv.y` if fins are mapped to specific UV regions).
            *   Apply sine wave-based displacement to fin vertices to simulate swimming motion. The player's current speed (`uPlayerSpeed`) should influence animation speed/intensity.
            *   Implement body bending/undulation when turning (`uIsTurning`, `uTurnDirection`).
    2.  **Fragment Shader (`clownfish.frag.ts`):**
        *   **Base Material:** Simulate a soft, slightly matte but vibrant material. Use basic Phong/Blinn-Phong lighting initially, but aim for a look that suggests light interacting with a slightly translucent organic surface.
        *   **Stripes:** Procedurally generate 2-3 white stripes. This can be done based on the Z-coordinate in object space. Create smooth transitions for stripe edges and add thin, dark outlines.
        *   **Fin Accents:** Color fin tips or edges using `uFinAccentColor`, possibly based on UV coordinates or vertex color attributes if set.
        *   **Eyes:** This is crucial.
            *   Define UV regions or use vertex attributes to identify eye areas.
            *   Render a dark `uEyePupilColor`.
            *   Render an `uEyeIrisColor` (could be a simple color or a subtle radial gradient).
            *   Implement a dynamic specular highlight (`uEyeHighlightColor`) that seems to react to light direction or camera position, giving the eyes a lively look. This can be a procedural dot placed on the iris.
        *   **Lighting Interaction:** Ensure the shader interacts well with the scene's `AmbientLight` and `DirectionalLight`.
*   **C. File Creation / Modification:**

    *   **Full File Path (New File):** `src/lib/game/shaders/character/clownfish.vert.ts`
    *   **File Content (Production-Ready Code):**
        ```typescript
        // src/lib/game/shaders/character/clownfish.vert.ts
        export const vertexShaderSource = `
          uniform float uTime;
          uniform float uPlayerSpeed; // Normalized speed factor
          uniform float uIsTurning;   // 0.0 or 1.0
          uniform float uTurnDirection; // -1.0 (left), 1.0 (right)

          // Animation parameters from config (passed as uniforms if varied per instance, or baked if const)
          uniform float uTailFinFrequency;
          uniform float uTailFinAmplitude;
          uniform float uPectoralFinFrequency;
          uniform float uPectoralFinAmplitude;
          // Could add more for dorsal, pelvic, anal fins

          // Attribute to identify parts (0: body, 1: tail, 2: L pectoral, 3: R pectoral, 4: dorsal, etc.)
          // This needs to be set when creating BufferGeometry for the fish parts
          attribute float aPartIndex; 

          varying vec3 vNormal;
          varying vec2 vUv;
          varying vec3 vWorldPosition;
          varying float vDepthForStripes; // Object-space Z for stripe calculation

          void main() {
            vUv = uv;
            vNormal = normalize(normalMatrix * normal);
            vDepthForStripes = position.z; // Assuming Z is along the fish's length before world transform

            vec3 animatedPosition = position;
            vec3 animatedNormal = normal; // Normals also need to be transformed by animation

            float speedFactor = 1.0 + uPlayerSpeed * 2.0; // Make animations faster with speed

            // --- Procedural Animation ---
            // Caudal (Tail) Fin Animation (assuming aPartIndex == 1.0)
            if (aPartIndex == 1.0) { 
              float tailAngle = sin(uTime * uTailFinFrequency * speedFactor) * uTailFinAmplitude * speedFactor;
              // Bend based on horizontal position relative to fin's local Z axis (animatedPosition.x)
              // This assumes tail fin geometry is oriented along Z axis in local space
              // And its width is along X. We want to sway it side-to-side (around Y axis).
              // The amount of sway depends on how far along the tail the vertex is (animatedPosition.z)
              float zNormalized = clamp(animatedPosition.z / 0.3, 0.0, 1.0); // Assuming tail length ~0.3
              float currentTailAngle = tailAngle * zNormalized * zNormalized; // Stronger bend at tip

              // Apply rotation around Y axis for side-to-side sway
              mat2 rotY = mat2(cos(currentTailAngle), -sin(currentTailAngle), 
                               sin(currentTailAngle),  cos(currentTailAngle));
              animatedPosition.xz = rotY * animatedPosition.xz;
              // Also rotate normal (simplified for small angles)
              // For more accuracy, transform normal by the same rotation matrix
              // animatedNormal.xz = rotY * animatedNormal.xz; 
            }

            // Pectoral Fin Animation (Left: aPartIndex == 2.0, Right: aPartIndex == 3.0)
            // Similar logic: flapping around an axis (likely local X or Z depending on fin orientation)
            if (aPartIndex == 2.0 || aPartIndex == 3.0) { // Pectoral Fins
                float flapAngle = sin(uTime * uPectoralFinFrequency * speedFactor + (aPartIndex == 2.0 ? 0.0 : 0.2)) * uPectoralFinAmplitude; // Offset phase for right fin
                // Assuming pectoral fins are oriented to flap around their local X-axis
                // And extend along their local Z-axis
                float finZNormalized = clamp(abs(animatedPosition.z) / 0.15, 0.0, 1.0); // Assuming fin length ~0.15 from its pivot
                float currentFlapAngle = flapAngle * finZNormalized;

                mat2 rotX = mat2(cos(currentFlapAngle), -sin(currentFlapAngle),
                                 sin(currentFlapAngle),  cos(currentFlapAngle));
                // If fin's length is along Z and it flaps up/down (around X), then YZ components rotate
                // This depends on how the fin geometry was initially created and oriented in ClownfishAsset.ts
                // Let's assume fins are created in XY plane, extending along Y, attached at Y=0, flapping around X
                // animatedPosition.yz = rotX * animatedPosition.yz; // Example
                // For side fins that flap:
                animatedPosition.xy = rotX * animatedPosition.xy; // If fin is in YZ plane, flaps around Z
            }
            
            // Body Bending/Turning Animation (aPartIndex == 0.0 for body)
            if (uIsTurning > 0.5 && aPartIndex == 0.0) {
                float turnInfluence = -position.z; // More bend towards the tail (positive Z in local space)
                turnInfluence = smoothstep(0.0, 0.5, turnInfluence); // Apply only to back half
                float bodyBendAngle = uTurnDirection * turnInfluence * 0.2; // Max bend angle

                mat2 bodyRotY = mat2(cos(bodyBendAngle), -sin(bodyBendAngle),
                                     sin(bodyBendAngle), cos(bodyBendAngle));
                animatedPosition.xz = bodyRotY * animatedPosition.xz;
            }


            vec4 worldPos = modelMatrix * vec4(animatedPosition, 1.0);
            vWorldPosition = worldPos.xyz;
            
            // Recompute normal based on animated position if complex deformation
            // For simpler rotations, transform normal by rotation matrix
            // For now, we pass the original normal, but this might need refinement for lighting on animated parts
            // vNormal = normalize(normalMatrix * animatedNormal); // More accurate

            gl_Position = projectionMatrix * viewMatrix * worldPos;
          }
        `;
        ```

    *   **Full File Path (New File):** `src/lib/game/shaders/character/clownfish.frag.ts`
    *   **File Content (Production-Ready Code):**
        ```typescript
        // src/lib/game/shaders/character/clownfish.frag.ts
        export const fragmentShaderSource = `
          #include <common>
          #include <packing>
          #include <lights_pars_begin> // For Three.js built-in lighting
          #include <fog_pars_fragment>
          #include <bsdfs> // For some advanced lighting functions if needed

          // Uniforms from ClownfishAsset material setup
          uniform vec3 uBaseColor;          // e.g., Orange
          uniform vec3 uStripeColor;        // e.g., White
          uniform vec3 uStripeEdgeColor;    // e.g., Black/Dark Grey
          uniform vec3 uFinAccentColor;     // e.g., Light Blue/Teal
          uniform vec3 uEyePupilColor;
          uniform vec3 uEyeIrisColor;
          uniform vec3 uEyeHighlightColor;
          uniform float uTime;

          varying vec3 vNormal;
          varying vec2 vUv; // Assuming UVs are set up for eyes/fins if needed
          varying vec3 vWorldPosition;
          varying float vDepthForStripes; // Object-space Z for stripes

          // Basic lighting model (can be expanded)
          vec3 calculateLighting(vec3 normal, vec3 viewDir, vec3 surfaceColor, vec3 lightDir, vec3 lightColor, vec3 ambientColor) {
              // Diffuse
              float NdotL = saturate(dot(normal, lightDir));
              vec3 diffuse = surfaceColor * lightColor * NdotL;

              // Specular (Simple Blinn-Phong)
              vec3 halfwayDir = normalize(lightDir + viewDir);
              float spec = pow(saturate(dot(normal, halfwayDir)), 32.0); // Shininess
              vec3 specular = lightColor * spec * 0.3; // Specular intensity

              return ambientColor * surfaceColor + diffuse + specular;
          }
          
          // Simple function to create soft stripe edges
          float smoothStripe(float value, float start, float end, float edgeWidth) {
              float halfWidth = (end - start) * 0.5;
              float center = start + halfWidth;
              float distToCenter = abs(value - center);
              return 1.0 - smoothstep(halfWidth - edgeWidth, halfWidth, distToCenter);
          }


          void main() {
            vec3 finalColor = uBaseColor;
            vec3 normal = normalize(vNormal);
            vec3 viewDir = normalize(cameraPosition - vWorldPosition); // For specular, etc.

            // --- Procedural Stripes ---
            // Define stripe regions based on vDepthForStripes (object-space Z)
            // Assuming fish body length is roughly -0.5 to 0.5 in local Z after scaling for this example
            // These values need careful tuning based on the actual model proportions.
            float stripeWidth = 0.1; // Width of white part
            float edgeWidth = 0.015; // Width of dark edge

            // Stripe 1 (near head)
            float stripe1_z_center = -0.25; 
            float stripe1_val = smoothStripe(vDepthForStripes, stripe1_z_center - stripeWidth/2.0, stripe1_z_center + stripeWidth/2.0, edgeWidth * 2.0); // Wider transition for dark edge
            float stripe1_dark_edge = smoothStripe(vDepthForStripes, stripe1_z_center - stripeWidth/2.0 - edgeWidth, stripe1_z_center + stripeWidth/2.0 + edgeWidth, edgeWidth);
            
            // Stripe 2 (middle)
            float stripe2_z_center = 0.05;
            float stripe2_val = smoothStripe(vDepthForStripes, stripe2_z_center - stripeWidth/2.0, stripe2_z_center + stripeWidth/2.0, edgeWidth * 2.0);
            float stripe2_dark_edge = smoothStripe(vDepthForStripes, stripe2_z_center - stripeWidth/2.0 - edgeWidth, stripe2_z_center + stripeWidth/2.0 + edgeWidth, edgeWidth);

            // Stripe 3 (near tail base)
            float stripe3_z_center = 0.3;
            float stripe3_val = smoothStripe(vDepthForStripes, stripe3_z_center - stripeWidth*0.7/2.0, stripe3_z_center + stripeWidth*0.7/2.0, edgeWidth * 2.0); // Thinner tail stripe
            float stripe3_dark_edge = smoothStripe(vDepthForStripes, stripe3_z_center - stripeWidth*0.7/2.0 - edgeWidth, stripe3_z_center + stripeWidth*0.7/2.0 + edgeWidth, edgeWidth);

            // Combine stripes:
            // Start with base color. Apply dark edge first, then white stripe on top.
            finalColor = mix(finalColor, uStripeEdgeColor, (1.0-stripe1_dark_edge) * 0.8); // 0.8 intensity for edge
            finalColor = mix(finalColor, uStripeColor, stripe1_val);

            finalColor = mix(finalColor, uStripeEdgeColor, (1.0-stripe2_dark_edge) * 0.8);
            finalColor = mix(finalColor, uStripeColor, stripe2_val);
            
            finalColor = mix(finalColor, uStripeEdgeColor, (1.0-stripe3_dark_edge) * 0.8);
            finalColor = mix(finalColor, uStripeColor, stripe3_val);


            // --- Eye Implementation (Simplified - assumes UVs map eye area to a quadrant, e.g., top-right) ---
            // A more robust way is to use a separate geometry for eyes or a dedicated texture map.
            // For procedural, we can use UVs or vertex attributes to identify eye regions.
            // Example: if UV.x > 0.8 and UV.y > 0.8 is the eye area on the texture map
            // This is highly dependent on how UVs are laid out for the fish model.
            // For this procedural example, let's assume a region based on vUv.
            // This part will need significant iteration with the actual model's UVs or vertex attributes.
            
            // Simplified eye logic (placeholder - this needs careful UV mapping or vertex attributes)
            // Let's assume body geometry has UVs where eyes could be:
            vec2 eyeCenterUV_L = vec2(0.25, 0.75); // Example UV center for left eye
            vec2 eyeCenterUV_R = vec2(0.75, 0.75); // Example UV center for right eye
            float eyeRadiusUV = 0.1;
            float pupilRadiusUV = 0.04;
            float irisRadiusUV = 0.08;

            float distToEyeL = distance(vUv, eyeCenterUV_L);
            float distToEyeR = distance(vUv, eyeCenterUV_R);

            bool isEye = false;
            vec2 currentEyeCenterUV = vec2(0.0);

            if (distToEyeL < eyeRadiusUV) {
                isEye = true;
                currentEyeCenterUV = eyeCenterUV_L;
            } else if (distToEyeR < eyeRadiusUV) {
                isEye = true;
                currentEyeCenterUV = eyeCenterUV_R;
            }

            if (isEye) {
                float distToCurrentEyeCenter = distance(vUv, currentEyeCenterUV);
                if (distToCurrentEyeCenter < pupilRadiusUV) {
                    finalColor = uEyePupilColor;
                } else if (distToCurrentEyeCenter < irisRadiusUV) {
                    // Simple iris color, can add gradient later
                    finalColor = uEyeIrisColor; 
                } else {
                    // Sclera (white part of eye) - can be mixed with uStripeColor for consistency
                    finalColor = uStripeColor * 0.9; 
                }

                // Specular Highlight for eye (procedural dot)
                // Position highlight based on view direction and a fixed light source for eyes
                vec3 eyeSurfaceNormal = normal; // Use mesh normal for simplicity
                vec3 eyeLightDir = normalize(vec3(0.5, 0.5, 1.0)); // Fixed light for eye highlight
                vec3 eyeReflected = reflect(-eyeLightDir, eyeSurfaceNormal);
                float eyeSpec = pow(saturate(dot(eyeReflected, viewDir)), 100.0); // Sharp highlight
                
                // Alternative: Place highlight procedurally as a dot based on UVs relative to eye center
                vec2 highlightOffset = vec2(0.015, 0.015); // Offset from pupil center
                float distToHighlight = distance(vUv - currentEyeCenterUV, highlightOffset);
                float highlightSpot = 1.0 - smoothstep(0.0, pupilRadiusUV * 0.3, distToHighlight);
                finalColor = mix(finalColor, uEyeHighlightColor, highlightSpot * 0.8 + eyeSpec * 0.5);
            }

            // --- Fin Accents (Example: darker tips for fins if aPartIndex was passed as varying) ---
            // varying float vPartIndex; // Would need this from vertex shader
            // if (vPartIndex > 0.5 && vPartIndex < 4.5) { // If it's a fin (excluding tail for this example)
            //    float finEdgeFactor = saturate(vUv.y * 2.0); // Assuming UV.y goes from 0 (base) to 1 (tip)
            //    finalColor = mix(finalColor, uFinAccentColor, finEdgeFactor * 0.7);
            // }


            // Apply basic Three.js lighting
            ReflectedLight reflectedLight = ReflectedLight(vec3(0.0), vec3(0.0), vec3(0.0), vec3(0.0));
            vec3 totalEmissiveRadiance = vec3(0.03); // Slight self-illumination for cartoony look

            // Using built-in lighting functions
            #if NUM_POINT_LIGHTS > 0
              for (int i = 0; i < NUM_POINT_LIGHTS; i++) {
                pointLightInterface pointLight = pointLights[i];
                getPointDirectLightIrradiance(pointLight, geometry, material, reflectedLight);
              }
            #endif
            #if NUM_DIR_LIGHTS > 0
              for (int i = 0; i < NUM_DIR_LIGHTS; i++) {
                directionalLightInterface directionalLight = directionalLights[i];
                getDirectionalDirectLightIrradiance(directionalLight, geometry, material, reflectedLight);
              }
            #endif
            #if NUM_SPOT_LIGHTS > 0
              for (int i = 0; i < NUM_SPOT_LIGHTS; i++) {
                spotLightInterface spotLight = spotLights[i];
                getSpotDirectLightIrradiance(spotLight, geometry, material, reflectedLight);
              }
            #endif
            #if NUM_HEMI_LIGHTS > 0
                for (int i = 0; i < NUM_HEMI_LIGHTS; i++) {
                    reflectedLight.indirectDiffuse += getHemisphereLightIrradiance(hemisphereLights[i], geometry);
                }
            #endif
            
            vec3 outgoingLight = reflectedLight.directDiffuse + reflectedLight.indirectDiffuse + 
                                 reflectedLight.directSpecular + reflectedLight.indirectSpecular + 
                                 totalEmissiveRadiance * finalColor; // Modulate emissive by base color

            gl_FragColor = vec4(outgoingLight * finalColor, 1.0); // Modulate lighting by procedurally determined color

            #include <tonemapping_fragment>
            #include <colorspace_fragment>
            #include <fog_fragment> // Apply fog if enabled in scene
          }
        `;
        ```
    *   **Explanation:**
        *   **Vertex Shader:**
            *   Passes UVs, normals, `vWorldPosition`.
            *   `vDepthForStripes`: Passes object-space Z coordinate for stripe calculation.
            *   Includes uniforms for animation parameters (`uTime`, `uPlayerSpeed`, fin frequencies/amplitudes).
            *   `aPartIndex` (attribute): This is crucial. When creating the `BufferGeometry` for the fish (body, each fin as separate parts merged or as separate meshes in a group), this attribute must be set per vertex to identify which part it belongs to. This allows the vertex shader to apply specific animations to specific parts.
            *   Animation logic: Basic sine-wave displacement for tail and pectoral fins. The displacement amount is scaled by the vertex's position along the fin (stronger at the tip) and player speed. Body bending for turns is also added.
        *   **Fragment Shader:**
            *   Includes Three.js lighting and fog chunks.
            *   Uniforms for all configurable colors.
            *   **Stripes:** Generated procedurally based on `vDepthForStripes`. `smoothStripe` function creates soft-edged stripes with darker outlines. The Z-coordinates for stripe centers (`stripe1_z_center`, etc.) will need careful tuning based on the final model proportions.
            *   **Eyes:** A simplified procedural eye implementation is provided as a starting point. This relies heavily on UV mapping the eye area to a known region or using a dedicated vertex attribute. For a true Pixar look, the eyes might eventually need separate, carefully UV-mapped geometry, or a more sophisticated shader/texture approach. The current example uses UV distance for pupil/iris and a procedural specular highlight.
            *   **Lighting:** Uses standard Three.js lighting contributions. `totalEmissiveRadiance` adds a bit of self-illumination for a softer, more cartoony look. The final color is a modulation of the calculated lighting and the procedurally determined surface color (base, stripes, eyes).
*   **I. Proactive Considerations & NFR Alignment:**
    *   **`aPartIndex` Attribute:** This is the most complex part of the setup. When creating the `ClownfishAsset` geometry, you'll need to combine the body and fin geometries (or keep them as separate `Mesh` objects within the `ClownfishAsset`'s `THREE.Group`). If merged, you must create and populate a `BufferAttribute` named `aPartIndex` for all vertices, assigning a unique float ID to vertices of each part (e.g., 0.0 for body, 1.0 for tail, 2.0 for left pectoral, etc.). This allows the vertex shader to selectively animate parts.
    *   **UV Mapping for Eyes/Fins:** While stripes are done with world Z, precise eye details and fin patterns (if any) would greatly benefit from careful UV unwrapping of the procedural mesh. For P1, we rely more on direct shader calculations.
    *   **Iteration:** The stripe Z-coordinates, eye UVs/logic, and animation parameters will require significant iteration and visual tweaking alongside the procedural geometry.
    *   **Performance:** Vertex shader animation is generally performant. The fragment shader complexity is moderate. Avoid too many complex calculations or texture lookups if not necessary.

---

#### Sub-Task 2.3: Register Clownfish Shader and Update `ClownfishAsset.ts` & `ProceduralAssetFactory.ts`

*   **A. Purpose & Rationale:**
    *   To make the new `clownfishShader` available through `ShaderManager` and update `ClownfishAsset` to use it. `ProceduralAssetFactory` will then use the enhanced `ClownfishAsset`.
*   **C. File Creation / Modification:**

    *   **Full File Path:** `src/lib/game/services/ShaderManager.ts`
    *   **Specific Changes (Register clownfish shader in constructor):**
        ```typescript
        // src/lib/game/services/ShaderManager.ts
        // ... (other imports)
        import { vertexShaderSource as clownfishVertex } from '../shaders/character/clownfish.vert';
        import { fragmentShaderSource as clownfishFragment } from '../shaders/character/clownfish.frag';
        // ...

        export class ShaderManager {
          constructor() {
            // ... (globalUniforms init, registerCoreChunks, registerTestPatternShader, registerSeafloorShader)
            const playerCfg = configSystem.get('player'); // For default animation params

            this.registerShader({
              name: 'clownfishShader',
              vertexShaderSource: clownfishVertex,
              fragmentShaderSource: clownfishFragment,
              defaultUniforms: () => ({ // Ensure fresh objects
                uBaseColor: { value: new THREE.Color(playerCfg.clownFishBaseColor) },
                uStripeColor: { value: new THREE.Color(playerCfg.clownFishStripeColor) },
                uStripeEdgeColor: { value: new THREE.Color(playerCfg.clownFishStripeEdgeColor) },
                uFinAccentColor: { value: new THREE.Color(playerCfg.clownFishFinAccentColor) },
                uEyePupilColor: { value: new THREE.Color(playerCfg.eyePupilColor) },
                uEyeIrisColor: { value: new THREE.Color(playerCfg.eyeIrisColor) },
                uEyeHighlightColor: { value: new THREE.Color(playerCfg.eyeHighlightColor) },
                // Animation uniforms - these will also be part of globalUniforms or updated by ClownfishAsset
                uPlayerSpeed: { value: 0.0 }, 
                uIsTurning: { value: 0.0 },
                uTurnDirection: { value: 0.0 },
                uTailFinFrequency: { value: playerCfg.tailFinFrequency },
                uTailFinAmplitude: { value: playerCfg.tailFinAmplitude },
                uPectoralFinFrequency: { value: playerCfg.pectoralFinFrequency },
                uPectoralFinAmplitude: { value: playerCfg.pectoralFinAmplitude },
              }),
              materialParameters: {
                fog: true, // Character should be affected by scene fog
                lights: true, // Character should react to scene lights
                side: THREE.FrontSide, // Usually FrontSide for solid objects
              }
            });
            console.log('ShaderManager: Registered clownfishShader.');
          }
          // ...
        }
        ```

    *   **Full File Path:** `src/lib/game/assets/character/ClownfishAsset.ts`
        *   **Specific Changes:** Ensure the `createMesh` method correctly requests and uses the `'clownfishShader'` from `ShaderManager`. The placeholder code already does this. **Crucially, implement the `aPartIndex` attribute when creating and merging geometries.**
            ```typescript
            // src/lib/game/assets/character/ClownfishAsset.ts
            // In createMesh, after creating bodyGeom, dorsalFinGeom, etc.
            // Before merging or adding to group, add the aPartIndex attribute:

            private addPartIndexAttribute(geometry: THREE.BufferGeometry, partId: number): void {
                const vertexCount = geometry.attributes.position.count;
                const partIndices = new Float32Array(vertexCount);
                partIndices.fill(partId);
                geometry.setAttribute('aPartIndex', new THREE.BufferAttribute(partIndices, 1));
            }

            // In createMesh():
            // ...
            const bodyGeom = this.createBodyGeometry();
            this.addPartIndexAttribute(bodyGeom, 0.0); // 0 for body

            const bodyMaterial = this.shaderManager.createShaderMaterial(
                'clownfishShader', 
                { /* Instance specific uniform overrides if any, not typically needed if defaults are good */ },
                { side: THREE.DoubleSide } // Example: Override material side for fins if they are planes
            );
            // ...
            const body = new THREE.Mesh(bodyGeom, bodyMaterial);
            // ...
            const dorsalFinGeom = this.createFinGeometry(0.7, 0.35, 5, 3, 'dorsal');
            this.addPartIndexAttribute(dorsalFinGeom, 4.0); // 4 for dorsal fin
            const dorsalFin = new THREE.Mesh(dorsalFinGeom, bodyMaterial); // Reuse material
            // ... and so on for all other fins, assigning unique part IDs.
            // TailFin: 1.0
            // Left Pectoral: 2.0
            // Right Pectoral: 3.0
            // Dorsal: 4.0
            // Left Pelvic: 5.0
            // Right Pelvic: 6.0
            // Anal Fin: 7.0
            ```

    *   **Full File Path:** `src/lib/game/assets/ProceduralAssetFactory.ts`
    *   **File Content (Modifications):**
        ```typescript
        // src/lib/game/assets/ProceduralAssetFactory.ts
        import { ClownfishAsset } from './character/ClownfishAsset'; // Add import

        export class ProceduralAssetFactory {
          // ... (shaderManager)
          private clownfishAssetGenerator!: ClownfishAsset; // Declare

          constructor(shaderManager: ShaderManager) {
            this.shaderManager = shaderManager;
            // ... (other asset generators)
            this.clownfishAssetGenerator = new ClownfishAsset(this.shaderManager); // Instantiate
            console.log("ProceduralAssetFactory: Initialized with ClownfishAsset.");
          }

          public createPlayerMesh(): THREE.Group { // Return the Group from ClownfishAsset
            // return this.clownfishAssetGenerator.getMesh(); // This now returns the fully styled clownfish
            // To ensure a new instance if ProceduralAssetFactory is long-lived and createPlayerMesh is called multiple times:
             const playerAsset = new ClownfishAsset(this.shaderManager); // Create new instance
             return playerAsset.getMesh();
          }
          
          // If ClownfishAsset needs access to PlayerController for animation updates:
          // The factory would just return the asset, and PlayerController would hold it.
          public createClownfishAsset(): ClownfishAsset {
              return new ClownfishAsset(this.shaderManager);
          }
          // ...
        }
        ```
    *   **Full File Path:** `src/lib/game/managers/PlayerController.ts`
    *   **File Content (Modifications):**
        ```typescript
        // src/lib/game/managers/PlayerController.ts
        import { ClownfishAsset } from '../assets/character/ClownfishAsset'; // Add import

        export class PlayerController {
          public mesh!: THREE.Group; // Player mesh is now a Group
          private clownfishAsset!: ClownfishAsset; // Store the asset instance
          // ...

          private initialize(): void {
            // this.mesh = this.assetFactory.createPlayerMesh(); // Old way
            this.clownfishAsset = this.assetFactory.createClownfishAsset();
            this.mesh = this.clownfishAsset.getMesh();
            // ...
          }
          
          public update(deltaTime: number): void {
            // ... (movement logic)

            // Update clownfish animation
            const currentSpeedNormalized = this.currentForwardSpeed / this.baseForwardSpeed; // Example normalization
            let isTurning = false;
            let turnDirection = 0;
            if (this.isTransitioningLane) {
                isTurning = true;
                turnDirection = Math.sign(this.targetLaneX - this.previousLaneX);
            }
            this.clownfishAsset.updateAnimation(deltaTime, currentSpeedNormalized, isTurning, turnDirection);

            // ... (invincibility logic)
          }
          
          // getCollisionObject might need refinement if the main mesh is a Group.
          // It should return a specific collision-representative mesh within the group, or a bounding box/sphere.
          // For now, assume the main body part is representative or a dedicated collider mesh is added to the group.
          public getCollisionObject(): THREE.Object3D { // Could be Mesh or Group
            // If ClownfishAsset's mesh group has a specific "collider" child:
            // return this.mesh.getObjectByName("PlayerCollider") || this.mesh; 
            return this.mesh; // For now, the whole group, collision system will use its bounding sphere.
          }
          // ...
        }
        ```
    *   **Explanation:**
        *   `ShaderManager` registers the `clownfishShader` with its default uniforms, sourcing colors and animation parameters from `configSystem`.
        *   `ClownfishAsset` is critically updated to add the `aPartIndex` attribute to its geometries. This is vital for the vertex shader to apply different animations to different parts. It now uses the `clownfishShader`.
        *   `ProceduralAssetFactory` now creates and returns an instance of the detailed `ClownfishAsset`.
        *   `PlayerController` instantiates `ClownfishAsset`, gets its mesh, and calls `clownfishAsset.updateAnimation()` in its own update loop, passing necessary parameters like speed and turning state.

---

#### Sub-Task 2.4: Iteration and Refinement of Geometry, Shaders, and Animations

*   **A. Purpose & Rationale:**
    *   This is an iterative process of tweaking procedural geometry parameters in `ClownfishAsset.ts`, adjusting GLSL shader code (`clownfish.vert.ts`, `clownfish.frag.ts`), and animation parameters in `gameConfig.ts` / `ClownfishAsset.ts` to achieve the desired Pixar-style look and feel, closely referencing the provided images.
*   **B. Implementation Details & Precise Instructions:**
    1.  **Geometry Iteration:**
        *   Adjust parameters in `createBodyGeometry` and `createFinGeometry` (radii, segments, deformation factors) to match the reference image proportions (rounded body, appropriate fin shapes and sizes).
        *   Focus on smooth transitions between body and fins.
        *   Ensure `aPartIndex` is correctly assigned if geometry structure changes.
    2.  **Shader Iteration (Fragment):**
        *   **Stripes:** Fine-tune Z-coordinates for stripe centers, stripe widths, and edge widths in `clownfish.frag.ts` until they match the reference (typically 3 distinct white stripes with thin black/dark edges).
        *   **Eyes:** This is the most important part for expressiveness.
            *   Refine the UV-based or attribute-based logic for identifying eye regions.
            *   Adjust `uEyePupilColor`, `uEyeIrisColor`. Consider adding a subtle radial gradient to the iris.
            *   Perfect the procedural specular highlight. It should be small, sharp, and appear in a consistent, believable location on the eye.
        *   **Material Quality:** Experiment with lighting calculations, add subtle rim lighting (Fresnel effect), or a very slight emissive component to make the character "pop" without looking flat or overly plastic.
            ```glsl
            // Example Fresnel for rim lighting in fragment shader
            // float fresnelTerm = pow(1.0 - saturate(dot(viewDir, normal)), 3.0);
            // finalColor += vec3(0.2) * fresnelTerm; // Add subtle white rim light
            ```
    3.  **Shader Iteration (Vertex - Animation):**
        *   Adjust `uTailFinFrequency`, `uTailFinAmplitude`, `uPectoralFinFrequency`, `uPectoralFinAmplitude` in `gameConfig.ts` and how they are used in `clownfish.vert.ts`.
        *   Refine the animation logic (sine waves, tapering of effect along fins) to make swimming motions look fluid and natural for a clownfish.
        *   Ensure animations scale appropriately with `uPlayerSpeed`.
        *   Refine body bending during turns.
*   **C. File Creation / Modification:** Primarily iterative changes within:
    *   `src/lib/game/assets/character/ClownfishAsset.ts` (geometry parameters)
    *   `src/lib/game/shaders/character/clownfish.vert.ts` (animation logic)
    *   `src/lib/game/shaders/character/clownfish.frag.ts` (stripes, eyes, material look)
    *   `src/lib/game/config/gameConfig.ts` (animation and color parameters)
*   **I. Proactive Considerations & NFR Alignment:**
    *   **Frequent Visual Checks:** Compile and run the game frequently to see the impact of changes.
    *   **Performance Profiling:** After significant shader changes, check performance. Complex fragment shaders can be costly.
    *   **Modularity in Shaders:** If parts of the shader logic become very complex (e.g., eye rendering), consider breaking them into more functions within the GLSL code or even more common chunks if reusable.

---

### 3. Comprehensive Testing and Validation Plan for THIS STEP

*   **A. Unit Testing:**
    *   **Test File Paths:** Potentially `src/lib/game/assets/character/ClownfishAsset.test.ts` (New)
    *   **Production-Ready Unit Test Code (Conceptual):**
        *   Unit testing procedural geometry generation can be complex. Focus on:
            *   `it('should create a mesh group with body and all fin meshes as children')`.
            *   `it('should assign correct aPartIndex attributes to different geometry parts')` (requires inspecting BufferAttributes).
            *   `it('should apply animation parameters from config to material uniforms')`.
    *   **Commands to Run Unit Tests:** `npm test -- src/lib/game/assets/character/ClownfishAsset.test.ts`
    *   **Expected Test Output & Coverage Goals:** Basic structural tests passing. Full visual validation is manual.
*   **B. Local Integration/Component Tests:** N/A beyond manual.
*   **C. Manual Verification Steps & Expected Visual/Functional Outcomes:**
    1.  `[ ]` Run the game.
    2.  `[ ]` **Overall Appearance:**
        *   **Expected:** The player character closely resembles the provided clownfish reference images in terms of body shape, proportions, fin shapes, and overall "Pixar-style" feel (rounded, appealing).
    3.  `[ ]` **Colors and Stripes:**
        *   **Expected:** Body is the correct orange (`clownFishBaseColor`). Three distinct white stripes (`clownFishStripeColor`) are present in the correct locations (head, mid-body, near tail base) with thin, dark edges (`clownFishStripeEdgeColor`). Stripe edges are smooth.
    4.  `[ ]` **Eyes:**
        *   **Expected:** Eyes are expressive. Clear pupil (`eyePupilColor`) and iris (`eyeIrisColor`). A distinct specular highlight (`eyeHighlightColor`) is visible and appears to react naturally to the scene's main light source or camera.
    5.  `[ ]` **Fins:**
        *   **Expected:** All fins (dorsal, pectoral, pelvic, anal, caudal) are present, correctly shaped, and positioned. Fin tips or edges may show `clownFishFinAccentColor`.
    6.  `[ ]` **Material Quality:**
        *   **Expected:** The fish's surface looks like a soft, slightly matte but vibrant organic material, not flat or overly plastic. It should interact convincingly with scene lighting.
    7.  `[ ]` **Procedural Animations:**
        *   **Expected:** Tail fin sways side-to-side. Pectoral fins "flap." Animations are fluid, continuous, and their speed/intensity increases with player movement speed. Body shows slight bending during lane changes.
    8.  `[ ]` **Performance:**
        *   **Expected:** Frame rate remains high and stable (target 60fps desktop, 30-45+ mobile) with the new character model and shaders. Use `stats.js`.
    9.  `[ ]` **Configurability:**
        *   Modify colors (`clownFishBaseColor`, `clownFishStripeColor`, etc.) and animation parameters (`tailFinFrequency`, etc.) in `gameConfig.ts`.
        *   **Expected:** Visuals and animations change accordingly in the game.
*   **D. UI/UX Validation Checklist:** N/A for direct UI changes, but character appeal is key to UX.
*   **E. Definition of "Done" for THIS SPECIFIC STEP:**
    1.  `[X]` `ClownfishAsset.ts` is implemented with advanced procedural geometry for body and all fins.
    2.  `[X]` `aPartIndex` attribute is correctly set on all character geometry parts.
    3.  `[X]` Custom GLSL shaders (`clownfish.vert.ts`, `clownfish.frag.ts`) are created and implement:
        *   `[X]` Procedural stripes with soft, dark edges.
        *   `[X]` Expressive eyes with pupil, iris, and dynamic specular highlight.
        *   `[X]` Desired soft material quality that interacts with scene lighting.
        *   `[X]` Procedural animations for fin and tail movement in vertex shader, influenced by player speed and turning.
    4.  `[X]` The `clownfishShader` is registered in `ShaderManager` and used by `ClownfishAsset`.
    5.  `[X]` `ProceduralAssetFactory` and `PlayerController` are updated to use the new `ClownfishAsset`.
    6.  `[X]` Manual verification confirms the character's visual appearance and animations closely match the target Pixar style and reference images.
    7.  `[X]` Performance with the new character is within acceptable limits.
    8.  `[X]` Code is committed to branch: `task/P1C-step5-clownfish-visuals`.
    9.  `[X]` NFRs (High visual quality, iconic look, performance, fluid animation) are demonstrably met.

### 4. Key Artifacts Produced & Project State After THIS STEP

*   **A. Manifest of New/Significantly Modified Files & Directories:**
    *   `src/lib/game/assets/character/ClownfishAsset.ts` (New/Heavily Modified)
    *   `src/lib/game/shaders/character/clownfish.vert.ts` (New)
    *   `src/lib/game/shaders/character/clownfish.frag.ts` (New)
    *   `src/lib/game/assets/ProceduralAssetFactory.ts` (Modified)
    *   `src/lib/game/managers/PlayerController.ts` (Modified for animation calls)
    *   `src/lib/game/services/ShaderManager.ts` (Modified to register new shader)
    *   `src/lib/game/config/gameConfig.ts` (Verified/Utilized player visual & animation params)
*   **B. Key Architectural Patterns/Decisions Applied or Reinforced in THIS STEP:**
    *   **Procedural First Asset Creation:** Reinforces the Phase 1 goal of creating core game assets purely through code and shaders.
    *   **Shader-Driven Appearance & Animation:** Complex visual details (stripes, eyes) and subtle animations are handled in GLSL, leveraging GPU capabilities.
    *   **Data-Driven Customization:** Key visual and animation parameters are exposed through `ConfigurationSystem` for easy tuning.
    *   **Attribute-Based Part Identification:** Using vertex attributes (`aPartIndex`) to differentiate parts of a single mesh for shader logic is a flexible technique.

### 5. Troubleshooting Common Issues & Proactive Error Prevention for THIS STEP

*   **Symptom:** Clownfish stripes are misaligned, have hard edges, or don't appear correctly.
    *   **Likely Cause(s):** Incorrect Z-coordinate ranges for stripes in `clownfish.frag.ts`; `vDepthForStripes` not correctly representing object-space Z; `smoothStripe` function parameters need tuning.
    *   **Solution/Debugging Tip:** In the fragment shader, temporarily output `vDepthForStripes` as a color (`gl_FragColor = vec4(vDepthForStripes, 0.0, 0.0, 1.0);`) to visualize its range. Adjust stripe center/width/edgeWidth values iteratively.
    *   **Prevention Note:** Establish clear local coordinate system conventions for the model (e.g., Z is always length).
*   **Symptom:** Eyes look flat, dead, or specular highlight is wrong.
    *   **Likely Cause(s):** UV mapping for eye region incorrect or not used effectively; procedural eye logic in fragment shader needs refinement; specular calculation incorrect or light direction not optimal.
    *   **Solution/Debugging Tip:** If using UVs for eyes, output `vUv` as color to debug mapping. Simplify eye shader to basic colors first, then add layers (iris, pupil, highlight). Test specular with a simple sphere first.
    *   **Prevention Note:** Eye appearance is highly sensitive. Iterate in small steps. Consider if separate eye geometry is eventually needed for more control (though P1 goal is procedural).
*   **Symptom:** Fin/tail animations are jerky, unnatural, or not working.
    *   **Likely Cause(s):** Incorrect `aPartIndex` values; math errors in vertex shader displacement logic; animation frequencies/amplitudes in `gameConfig.ts` poorly tuned; `uTime` or `uPlayerSpeed` uniforms not updating correctly.
    *   **Solution/Debugging Tip:** In vertex shader, output `aPartIndex` or displacement amounts as color to debug. Isolate animation for one fin type at a time. Log uniform values.
    *   **Prevention Note:** Start with very simple animations (e.g., one fin moving on one axis). Ensure vertex attributes are correctly passed.
*   **Symptom:** Character looks dark or lighting interaction is poor.
    *   **Likely Cause(s):** Normals not correctly computed/transformed after procedural deformation/animation; fragment shader lighting model too basic or incorrect; material parameters (diffuse, specular colors) not set well.
    *   **Solution/Debugging Tip:** Visualize normals (`gl_FragColor = vec4(normalize(vNormal) * 0.5 + 0.5, 1.0);`). Ensure `normalMatrix` is used for normal transformation in vertex shader if modelMatrix involves non-uniform scaling. Check light colors/intensities from `LightingManager`.
    *   **Prevention Note:** Always recompute or correctly transform normals after vertex manipulation. Use standard lighting functions from Three.js (`lights_phong_fragment`, etc.) as a base.

### 6. AI Self-Critique & Final Quality Assurance Checklist (for THIS STEP's Output)

1.  **Step Focus & Exclusivity:** Yes, fully focused on the clownfish character visuals and basic procedural animation.
2.  **Actionability & Completeness:** Yes, provides detailed structure for `ClownfishAsset.ts`, complete GLSL shader code, integration points, and configuration updates.
3.  **Production-Grade Code Quality:** Yes, shader code includes comments and uses standard practices. Asset class structure is modular. Emphasizes NFRs like performance.
4.  **Correctness & Functionality:** Procedural geometry and shader logic are standard approaches. Animation logic in vertex shader is a valid technique.
5.  **Dependency Management:** No new external dependencies. Correct internal dependencies identified.
6.  **File Paths & Project Structure Adherence:** Yes, all new/modified files align with the defined project structure.
7.  **UI/UX Excellence (if applicable):** While not direct UI, the character's visual appeal is a core UX component. Guidance aims for this.
8.  **NFR Adherence & Proactive Considerations:** High visual quality, iconic look, performance, and fluid animation are central to the provided solution. Iteration is stressed.
9.  **Testing & Validation Thoroughness:** Manual visual verification is key and detailed. Conceptual unit tests are outlined.
10. **Unambiguity & Precision:** Instructions are specific, especially regarding shader logic and the `aPartIndex` attribute.
11. **Proactive Problem Solving & Error Prevention:** Troubleshooting addresses common issues in procedural modeling and shader development.
12. **Alignment with ALL Inputs:** Yes, directly implements Step 5 of the blueprint, using provided reference images as the target.
13. **No Oversights (The "Last Mile"):** Includes the critical `aPartIndex` attribute setup, animation parameter integration from config, and considerations for eye/stripe details.

---
This completes the detailed plan for Step 5. This is a significant artistic and technical challenge, requiring close iteration between procedural geometry generation in TypeScript and GLSL shader development to achieve the target Pixar-style clownfish. The focus on expressive eyes and fluid, subtle animations will be key to its success.

